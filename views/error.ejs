<% layout("layouts/boilerplate") %>

<div class="row mt-5">
  <div class="alert alert-danger col-6 offset-3 text-center" role="alert">
    <h4 class="alert-heading"><%= err.statusCode || 500 %> Error</h4>
    <p><%= err.message || "Something went wrong." %></p>
    <hr />
    <a href="/listings" class="btn btn-dark mt-2">Back to Listings</a>
  </div>
</div>
